{
  "name": "link-saver",
  "version": "1.0.0",
  "private": true,
  "description": "A small single-page application for saving links.",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/backend/server.js",
    "dev": "node --watch src/backend/server.js",
    "test": "node --test",
    "lint": "node --check src/backend/app.js && node --check src/backend/server.js && node --check src/frontend/app.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
